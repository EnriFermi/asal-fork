# Legacy Entrypoints

These shell scripts were previously scattered in the repository root. They are kept here for provenance and quick manual reruns.

For new experiments, prefer:

```text
experiments/<domain>/<stage>/config.yaml
experiments/<domain>/<stage>/run.sh
```

The scripts in this folder may still encode old paths or historical parameter choices. Treat them as examples, not canonical paper-check entrypoints.

